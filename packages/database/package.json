{
  "name": "@edutech/database",
  "version": "1.0.0",
  "description": "Centralized database schema, Prisma client, migrations and seeds",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@edutech/common": "*",
    "@prisma/client": "^6.4.1",
    "argon2": "^0.41.1",
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "prisma": "^6.4.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
